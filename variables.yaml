
- name: print the message
  hosts: mongodb
  vars:
    name: redddy
    course: devops
  tasks: 
  - name: display message
    ansible.builtin.debug:
      msg: "hello world my name is {{name}} and i am learning {{course}}"
